# Contributing

We welcome Pull Requests! To get started:
1. Fork the repo and create your feature branch: `git checkout -b feature/my-new-feature`
2. Commit your changes. Make sure your commit syntax passes our pre-commit lint validation.
3. Push to the branch and submit a PR against `main`.

Please ensure all tests and coverage metrics pass locally (`npm run test:coverage`) before requesting review.
